# Backend CI Placeholder
name: Backend CI & Staging CD

on:
  push:
    branches:
      - main
      - develop
  pull_request:
    branches:
      - main
      - develop

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r backend/requirements.txt

      - name: Lint Code
        run: |
          flake8 --config=backend/.flake8 backend/app backend/tests

      - name: Run Tests
        env:
          PYTHONPATH: .
        run: |
          pytest backend/tests -v --cov=backend/app

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build Docker Image
        uses: docker/build-push-action@v5
        with:
          context: ./backend
          file: ./backend/Dockerfile
          platforms: linux/amd64
          push: false
          tags: drfirst-backend:ci-${{ github.sha }}

      - name: Push Docker Image to GCR/GAR
        run: |
          echo "# TODO: Implement Docker image push (Task 11.1.4)"
          echo "# This step will be conditional on push to main/develop branches"
          # gcloud auth configure-docker us-central1-docker.pkg.dev
          # docker tag drfirst-backend:${{ github.sha }} us-central1-docker.pkg.dev/drfirst-business-case-gen/drfirst-backend/drfirst-backend:${{ github.sha }}
          # docker push us-central1-docker.pkg.dev/drfirst-business-case-gen/drfirst-backend/drfirst-backend:${{ github.sha }} 