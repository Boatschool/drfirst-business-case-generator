name: Frontend CI & Firebase CD

on:
  push:
    branches:
      - main
      - develop
  pull_request:
    branches:
      - main
      - develop

jobs:
  build-test-deploy:
    runs-on: ubuntu-latest
    
    # Set default working directory for all run steps in this job
    defaults:
      run:
        working-directory: ./frontend

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18.x'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: Install Dependencies
        run: npm ci

      - name: Lint Code
        run: npm run lint:prod

      - name: Run Tests
        run: npm test

      - name: Build Application
        run: |
          echo "# TODO: Implement build process (Task 11.2.3)"
          # npm run build

      - name: Deploy to Firebase Hosting
        run: |
          echo "# TODO: Implement Firebase deployment (Task 11.2.4)"
          echo "# This step will be conditional on push to main/develop branches"
          # firebase deploy --only hosting 