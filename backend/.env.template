# DrFirst Business Case Generator - Backend Environment Variables Template
#
# INSTRUCTIONS:
# 1. Copy this file to .env in the same directory
# 2. Replace all placeholder values with your actual credentials
# 3. For production, copy this to .env.production and update values accordingly
#
# SECURITY NOTE: Never commit actual .env files to version control!

# ============================================================================
# APPLICATION SETTINGS
# ============================================================================

# Application environment (development, staging, production)
ENVIRONMENT=development

# Enable debug mode (true for development, false for production)
DEBUG=true

# Application secret key (generate a secure random string for production)
SECRET_KEY=your-secure-secret-key-change-this-in-production

# Logging level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# ============================================================================
# CORS CONFIGURATION
# ============================================================================

# Allowed CORS origins (comma-separated list)
# Development example:
BACKEND_CORS_ORIGINS=http://localhost:4000,http://localhost:4002,http://127.0.0.1:4000,http://127.0.0.1:4002

# Production example:
# BACKEND_CORS_ORIGINS=http://localhost:4000,http://localhost:4002,https://drfirst-business-case-gen.web.app,https://drfirst-business-case-gen.firebaseapp.com

# ============================================================================
# GOOGLE CLOUD PLATFORM CONFIGURATION
# ============================================================================

# GCP Project ID
GOOGLE_CLOUD_PROJECT_ID=drfirst-business-case-gen

# Path to GCP service account key file
# For local development, download service account key and set path:
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/your/service-account-key.json
# For production (Cloud Run), this is typically not needed as it uses IAM roles
GOOGLE_APPLICATION_CREDENTIALS=

# ============================================================================
# FIREBASE CONFIGURATION
# ============================================================================

# Firebase Project ID (should match GCP project ID)
FIREBASE_PROJECT_ID=drfirst-business-case-gen

# Firebase API Key (optional for server-side operations)
# Get this from Firebase Console → Project Settings → General → Web API Key
FIREBASE_API_KEY=your-firebase-web-api-key-here

# ============================================================================
# VERTEX AI CONFIGURATION
# ============================================================================

# Vertex AI location/region
VERTEX_AI_LOCATION=us-central1

# Vertex AI model name
VERTEX_AI_MODEL_NAME=gemini-2.0-flash-lite

# Model parameters (optional - will use defaults if not specified)
VERTEX_AI_TEMPERATURE=0.6
VERTEX_AI_MAX_TOKENS=4096
VERTEX_AI_TOP_P=0.9
VERTEX_AI_TOP_K=40

# ============================================================================
# RATE LIMITING (OPTIONAL)
# ============================================================================

# Redis URL for distributed rate limiting (optional)
# If not provided, uses in-memory rate limiting
# REDIS_URL=redis://localhost:6379

# Default rate limits
DEFAULT_RATE_LIMIT=100/minute
BURST_RATE_LIMIT=20/second

# ============================================================================
# SETUP INSTRUCTIONS
# ============================================================================

# FOR LOCAL DEVELOPMENT:
# 1. Download GCP Service Account Key:
#    - Go to GCP Console → IAM & Admin → Service Accounts
#    - Create or select service account with these roles:
#      * Firebase Admin SDK Administrator Service Agent
#      * Vertex AI User
#      * Firestore Service Agent
#    - Create key (JSON format)
#    - Download and place in secure location
#    - Set GOOGLE_APPLICATION_CREDENTIALS to file path
#
# 2. Get Firebase API Key:
#    - Go to Firebase Console → Project Settings → General
#    - Scroll to "Your apps" section
#    - Copy "Web API Key"
#    - Set FIREBASE_API_KEY
#
# FOR PRODUCTION DEPLOYMENT:
# 1. Use environment variables in Cloud Run (not service account file)
# 2. Ensure Cloud Run service has proper IAM roles:
#    - Firebase Admin
#    - Vertex AI User  
#    - Firestore Service Agent
# 3. Use strong SECRET_KEY (generate with: openssl rand -hex 32)
# 4. Set appropriate CORS origins for your domain
# 5. Set DEBUG=false and LOG_LEVEL=INFO

# ============================================================================
# EXAMPLE PRODUCTION VALUES
# ============================================================================

# ENVIRONMENT=production
# DEBUG=false
# SECRET_KEY=your-super-secure-production-secret-key
# LOG_LEVEL=INFO
# BACKEND_CORS_ORIGINS=https://drfirst-business-case-gen.web.app,https://drfirst-business-case-gen.firebaseapp.com
# GOOGLE_APPLICATION_CREDENTIALS=  # Leave empty for Cloud Run with IAM roles 