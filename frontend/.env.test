# Test Firebase Configuration
# Use this temporarily if you need to create your own Firebase project for testing

VITE_API_BASE_URL=http://localhost:8000
VITE_API_VERSION=v1

# Replace these with your own Firebase project credentials:
VITE_FIREBASE_API_KEY=your-api-key-here
VITE_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your-project-id

VITE_ENVIRONMENT=development
VITE_ENABLE_ANALYTICS=false
VITE_ENABLE_DEBUG_LOGGING=true

# Instructions to create your own Firebase project:
# 1. Go to https://console.firebase.google.com/
# 2. Click "Create a project"
# 3. Enter project name (e.g., "my-test-project")
# 4. Go to Authentication → Sign-in method → Google → Enable
# 5. Copy the config values from Project Settings → General → Your apps
# 6. Replace the values above
# 7. Rename this file to .env (backup the original first) 